% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_predictions_round_csv.R
\name{export_predictions_round_csv}
\alias{export_predictions_round_csv}
\title{export_predictions_round_csv}
\usage{
export_predictions_round_csv(path)
}
\arguments{
\item{path}{A file path (including \code{.csv} extension) where the exported
predictions should be written.}
}
\value{
Invisibly returns the data frame that was written to the CSV.
}
\description{
Exports the authoritative round-level predictions stored in the package
database (written by the skill pipeline) to a CSV file. This function
does \emph{not} modify the database in any way; it simply reads the
\code{predictions_round} table and writes it to disk.
}
\details{
Export round-level predictions to CSV

The skill pipeline (\code{golf::run_skill_pipeline()}) is the sole writer to the
\code{predictions_round} table inside the package DuckDB database. This export
function provides a convenient way to extract those predictions for
external analysis, archival, or sharing.

Unlike earlier versions of the export utilities, this function:
\itemize{
\item does \emph{not} create or alter any database tables,
\item does \emph{not} delete or overwrite prediction rows,
\item does \emph{not} inject metadata such as model version or timestamps,
\item does \emph{not} write anything back to the database.
}

It is a pure read → write CSV utility.
}
\examples{
\dontrun{
  export_predictions_round_csv("predictions_export.csv")
}

}
