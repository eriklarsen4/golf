# Ignore renv library (local machine-specific)
renv/library/

# RStudio junk
.Rproj.user
.Rhistory
.RData
.Ruserdata

# macOS junk
.DS_Store

# Git metadata (never needed in build context)
.git
.gitignore